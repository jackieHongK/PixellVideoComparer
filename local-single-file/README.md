# Local Single-File Build

This folder keeps the downloadable single-file comparer as a separate maintenance track.

- `comparer_local_singlefile.html`: preserved single HTML build for local use
- Primary web-hosting work happens in the repository root `index.html` and `comparer_dev_qa.html`
- Promote changes into this folder only when intentionally maintaining the local build
